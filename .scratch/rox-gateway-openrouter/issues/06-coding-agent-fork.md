# 06 — ROX coding-agent fork snapshot

**What to build:** Named branch and semantic commits from the detached dirty worktree (~3548 files). Not demoable from this cloud workspace.

**Blocked by:** Mac workspace / local ROX fork available.

**Status:** blocked

- [ ] Host with the dirty worktree is available
- [ ] Named branch, semantic commits, no `git stash` if that repo forbids it
