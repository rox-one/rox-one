# OEM kernel extraResources

This directory holds the staged `knowledge-engine` binary copied by
`apps/electron/scripts/stage-oem-kernel.sh` from `$OEM_KERNEL_PAYLOAD_DIR`.

Binaries are gitignored. Only `.gitkeep` and this README are tracked.
Do not commit tarballs or unpacked kernel UI here.
