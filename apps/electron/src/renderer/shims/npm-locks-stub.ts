/** Renderer stub: 893KB lockfile blobs stay in main/server. */
export function getNpmLock(_tool: string, _version: string): string | null {
  return null
}
export const OPENCLAW_NPM_PIN = { name: '', version: '' }
