# CF-1 status

- **Date:** 2026-08-13
- **Verdict:** Already landed on this tree. Not re-implemented. Tests re-run.

## Surfaces

| Module | Path | Role |
|---|---|---|
| Envelope codec | `packages/shared/src/credentials/envelope.ts` | Versioned `rox-credential-envelope` v1; legacy wrap in memory |
| Envelope tests | `packages/shared/src/credentials/__tests__/envelope.test.ts` | Scalar-only payload, stable fingerprint, tamper fail-closed |
| Metadata registry | `packages/core/src/platform/identity/credential-types.ts` | `CredentialRef` / versions; rejects `value` field |
| Registry tests | `packages/core/src/platform/identity/credential-types.test.ts` | No payload in metadata |

## Verification (this session)

```text
bun test packages/shared/src/credentials/__tests__/envelope.test.ts \
         packages/core/src/platform/identity/credential-types.test.ts
# 11 pass / 0 fail / 27 expect()  [49.00ms]
```

Legacy `CredentialManager.get` / `StoredCredential.value` read path remains. CF-2+ not opened.

## Increment A follow-on (2026-08-13 continue)

Peer TLS trust is now enforced before handshake:

- `packages/server-core/src/transport/peer-trust.ts`
- `WsRpcClient` awaits `peerTrustVerifier` before sending token-bearing handshake
- `tlsRejectUnauthorized: false` removed from client, Electron preload, and `connectToRemote`

```text
bun test packages/server-core/src/transport/__tests__/peer-trust.test.ts
         packages/shared/src/config/__tests__/remote-tls-trust.test.ts
         packages/shared/src/config/__tests__/remote-server-ssh-persistence.test.ts
# included in 27 pass / 0 fail with CF-1
```

## Increment A Task 3 (enrollment)

- `inspectRemoteTlsPeer` is a token-free TLS probe
- accept persists the inspected pin; reject persists nothing; pin change needs `confirm-rollover`
- `makeRemoteClient` / `connectToRemote` attach `peerTrustVerifier` except SSH tunnels
- Global `certificate-error` accept for `CRAFT_SERVER_URL` removed
- IPC: `remoteTls:inspect`, `remoteTls:decide`

```text
bun test apps/electron/src/main/__tests__/remote-tls-enrollment.test.ts
# 5 pass / 0 fail
```

## Increment B Task 4 (messaging access)

Legacy `open` / `inherit` / missing modes normalize to `public-inbox` (no session/tools). Execution requires `owner-control` plus owner or allow-list. Fresh bindings write `owner-control`.

```text
bun test packages/messaging-gateway/src/__tests__
# 224 pass / 0 fail
```
