# Статус CF-1

- **Дата:** 2026-08-13
- **Вердикт:** Уже вошёл в это дерево. Не переимплементирован. Тесты перезапущены.

## Поверхности

| Модуль | Путь | Роль |
|---|---|---|
| Кодек конверта | `packages/shared/src/credentials/envelope.ts` | Версионированный `rox-credential-envelope` v1; устаревшая обёртка в памяти |
| Тесты конверта | `packages/shared/src/credentials/__tests__/envelope.test.ts` | Полезная нагрузка только из скаляров, стабильный отпечаток, fail-closed при подделке |
| Реестр метаданных | `packages/core/src/platform/identity/credential-types.ts` | `CredentialRef` / версии; отклоняет поле `value` |
| Тесты реестра | `packages/core/src/platform/identity/credential-types.test.ts` | Нет полезной нагрузки в метаданных |

## Проверка (в этой сессии)

```text
bun test packages/shared/src/credentials/__tests__/envelope.test.ts \
         packages/core/src/platform/identity/credential-types.test.ts
# 11 pass / 0 fail / 27 expect()  [49.00ms]
```

Устаревший путь чтения `CredentialManager.get` / `StoredCredential.value` сохраняется. CF-2+ не открыт.

## Дополнение к Increment A (2026-08-13, продолжение)

Доверие к TLS-пиру теперь принудительно проверяется до рукопожатия:

- `packages/server-core/src/transport/peer-trust.ts`
- `WsRpcClient` ожидает `peerTrustVerifier` перед отправкой рукопожатия с токеном
- `tlsRejectUnauthorized: false` удалён из клиента, preload-скрипта Electron и `connectToRemote`

```text
bun test packages/server-core/src/transport/__tests__/peer-trust.test.ts
         packages/shared/src/config/__tests__/remote-tls-trust.test.ts
         packages/shared/src/config/__tests__/remote-server-ssh-persistence.test.ts
# included in 27 pass / 0 fail with CF-1
```

## Increment A, задача 3 (регистрация)

- `inspectRemoteTlsPeer` — TLS-проба без токена
- accept сохраняет проверенный пин; reject не сохраняет ничего; для смены пина нужен `confirm-rollover`
- `makeRemoteClient` / `connectToRemote` подключают `peerTrustVerifier`, кроме SSH-туннелей
- Глобальный accept события `certificate-error` для `CRAFT_SERVER_URL` удалён
- IPC: `remoteTls:inspect`, `remoteTls:decide`

```text
bun test apps/electron/src/main/__tests__/remote-tls-enrollment.test.ts
# 5 pass / 0 fail
```

## Increment B, задача 4 (доступ к обмену сообщениями)

Устаревшие режимы `open` / `inherit` / отсутствующий режим нормализуются в `public-inbox` (без session/tools). Для выполнения требуется `owner-control` плюс владелец или allow-list. Новые привязки записывают `owner-control`.

```text
bun test packages/messaging-gateway/src/__tests__
# 224 pass / 0 fail
```
