# Статус CF-4

- **Дата:** 2026-08-13
- **Вердикт:** Частично. CF-4.1 + CF-4.2 зелёные на этом дереве. Не CF-5.

## Поверхности

| Модуль | Путь | Роль |
|---|---|---|
| Брокер | `packages/shared/src/credentials/fabric/broker.ts` | `InProcessCredentialBroker`; запрет по умолчанию; `perform` один раз |
| Хранилище грантов | `packages/shared/src/credentials/fabric/grant-store.ts` | Память + JSON-метаданные; секретные поля отклоняются |
| Доставка | `packages/shared/src/credentials/fabric/delivery.ts` | Ранжированные механизмы; `env-legacy` только по явному включению |
| Тесты | `fabric/__tests__/broker.test.ts`, `broker-policy.test.ts` | Разрешение/запрет/отзыв + сохранение/восстановление/доставка |

## Проверка

```text
bun test packages/shared/src/credentials
# 40 pass / 0 fail
cd packages/shared && bun run tsc --noEmit
# clean
```

## Ещё не готово

UI/RPC подключений (CF-6), вертикаль GitHub (CF-7), вспомогательные бинарники, Hermes APPLY, Gate 0. CF-5 уже есть на этом дереве.
