# Статус CF-7

- **Дата:** 2026-08-13
- **Вердикт:** Частично. CF-7.1: импорт env из GitHub → Connection → выдача (grant) → брокеризованный `/user` → отзыв (revoke). CF-7.2 (UI импорта env) и CF-7.3 (подтверждённый отзыв) находятся на странице Connections. Реальный GitHub не задействован.

## Компоненты

| Модуль | Путь | Роль |
|---|---|---|
| Vertical | `packages/server-core/src/workgraph/github-vertical.ts` | импорт копированием GH_TOKEN/GITHUB_TOKEN + lease + `/user` |
| Тест | `github-vertical.test.ts` | инъектированный fetch; токен никогда не попадает в результирующий JSON |

## Проверка

```text
bun test packages/server-core/src/workgraph
# 20 pass / 0 fail
```

## Остаётся за рамками

OAuth/GitHub App, UI импорта на странице Connections, реальный api.github.com, Infisical (CF-8).
