# Ранбук A-ops (A0 FAIL CLOSED)

- **Дата:** 2026-08-19
- **Статус:** Владелец отправил точный токен `АПPLY HMA-20260809-A1` 2026-08-19. Инициализация создала доказательства с доступом только для владельца, затем завершилась по fail closed ещё до A0, поскольку 15/22 контрольных сумм аудита не совпали.
- **Авторитетный план:** `~/hermes-migration-audit-20260809-121222/11-apply-plan.md`
- **Worktree:** `/Users/marklindgreen/Projects/_craft_worktrees/do-it-all-security-slices`
- **Исторические доказательства R3:** коммит `78df77a`, исторический манифест на 35 путей SHA-256 `e9db5372da46fe146815150aca9b9d4e77b32fb8495a35b30d55013228359e35`, 173 сфокусированных теста до генерации его манифеста.
- **Текущий исходный код R3:** коммиты `feaced2`, `4dc9b1dc`, `e505d80f`, `cb36c864`, `b16cc69f`, `98f2f2c2` и `a840f27b` исправляют офлайн-контракты и границы безопасности covered-byte. Тесты/контрольные суммы/верификатор сознательно отложены по указанию владельца; ни стартовый токен, ни действие A0 не подразумеваются.

## Живой FAIL CLOSED (не мутировать)

| Проверка | Текущее значение | Требуется перед мутацией |
|---|---|---|
| Артефакт APPLY `~/.hermes-migration-apply/HMA-20260809-A1` | присутствует: корень `0700`, `apply.log` `0600`, `INIT audit-checksums FAIL` | Ремедиация Revision-3 и новое одобрение владельца перед повторной попыткой |
| `hermes config get approvals.mode` | `off` | `APPROVE A2 HERMES SMART` после APPLY + A1 |
| `hermes config get security.tirith_fail_open` | `true` | тот же шлюз A2 |
| Именованный источник/цель Tailscale | не определено | `APPROVE D0 TARGET <device> <node-id>` |

Ни одна команда живого обнаружения Hermes/Tailscale не запускалась. Ни один инструмент не копировался. Не выполнялись `hermes config set`, chmod, резервное копирование, остановка gateway, Syncthing, ACL, обмен сообщениями или ротация секретов.

## Свидетельства fail-closed для A0 — 2026-08-19

- `mtime` файла `checksums.sha256`: `2026-08-09T13:41:49Z`; 15 из 22 записей не совпадают с текущими файлами, все 22 являются обычными файлами и ни один не является симлинком.
- Более поздние mtime файлов объясняют механический характер сбоя, но не устанавливают безопасное происхождение. Сам манифест хешируется в `2c242de9a2243621992b46834520529e207daaaff16b2993d449866db44578c8`.
- Корень apply: `0700`; лог: `0600`; каталог tools: `0700` и пуст; единственный статус в логе — `INIT audit-checksums FAIL`.
- Независимый аудит плана выявил пропуски контрольных сумм, выдачу токенов без учёта ревизии, неразрешённую цель, противоречия между каналом и классом секрета, некорректное состояние покоя Buzz, неполные шлюзы/откат и устаревшие предположения о живом окружении.
- Вердикт независимого аудита инструментов: **ТРЕБУЕТ ИСПРАВЛЕНИЙ** (5 высоких, 6 средних). Не пересчитывать контрольные суммы текущих байтов. Требуемые исправления охватывают Git-изоляцию обнаружения, консервативные классы секретов, детектирование SQLite/WAL, очистку открытого текста, полноту манифеста, схему/атомарность SharedMemory, canary CLI, восстановление режима no-follow, верификацию закреплённого бандла и точные вызовы safe-restore/Syncthing-guard.
- Ремедиация Revision-3 одобрена и внедрена. Следующее возможное действие — один чистый прогон верификатора неизменяемого бандла, затем точный новый стартовый токен `АПPLY HMA-20260809-A1-R3`; A1/A2/A3 остаются заблокированы.

## Никогда

- Восстанавливать `approvals.mode=off`.
- `--yolo`, Funnel, SQLite-over-network.
- Выводить или искать содержимое `tskey-api.secret`. Ротация — только как отдельная операция с гейтом владельца.
- Зондировать выведенный из эксплуатации узел helsinki.

## Порядок этапов (копировать из плана apply; останавливаться на каждом подтверждении)

1. **A0: дрейф только для чтения** — только после чистого верификатора R3 и точного `АПPLY HMA-20260809-A1-R3`; использовать команду `discover_surface.py --source-dir ... --dest-dir ... --expected-root ...` из неизменяемого плана apply. Откат: нет.
2. **A1: резервное копирование** — ждать `APPROVE A1 BACKUP`. Откат источника с шифрованием через `build_secure_state.py` + age. Fail closed, если резервное копирование завершается неудачей.
3. **A2: Hermes smart** — ждать `APPROVE A2 HERMES SMART`. Затем только:
   - `security.redact_secrets=true`
   - `security.tirith_enabled=true`
   - `security.tirith_fail_open=false`
   - `security.allow_private_urls=true`
   - `approvals.mode=smart`
   - `approvals.destructive_slash_confirm=true`
   - `approvals.mcp_reload_confirm=true`
   - `skills.write_approval=false`
   - `memory.write_approval=false`
   - проверить с помощью `hermes config get` + `hermes doctor`
   - откат A2: вернуть прежние значения через `hermes config set`; **никогда** не восстанавливать `off` или fail-open у Tirith.
4. **A3: права доступа** — ждать `APPROVE A3 PERMISSIONS`. Сделать снимок режимов, выставить chmod только для владельца на хранилищах Hermes/OMP, перечисленных в плане apply, затем `hermes gateway start` / `resume`. Откат — только через `mode_manifest.py --restore`.

Последующие D0/D1/F0+ остаются заблокированными до появления именованного узла Tailscale и отдельных одобрений.

## Определение готовности для этого документа

Чек-лист существует, живые шлюзы зафиксированы, никакие мутации Hermes/файловой системы не выполнялись.
