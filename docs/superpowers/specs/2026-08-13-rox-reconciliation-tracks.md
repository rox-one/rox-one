# ROX reconciliation — track split

Дата: 2026-08-13. Это **не** один implementation spec. Три независимых подпроекта.

| Track | Repo / host | Blocker here | Spec |
|-------|-------------|--------------|------|
| 1 Gateway catalog honesty | `agisota/zed-api` `release/v3.8.50` | GitHub write + staging host | `2026-08-13-rox-gateway-openrouter-catalog-design.md` |
| 2 Coding-agent fork | local ROX fork, Mac worktree | workspace not mounted | none until that host |
| 3 Daily upstream sync | local fork + launchd | same | none until that host |

Не смешивать в одном PR, одном плане и одном агентном прогоне.
