---
- name: Reload systemd
  listen: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart infisical-agent
  listen: restart infisical-agent
  ansible.builtin.systemd:
    name: infisical-agent
    state: restarted
  when: not ansible_check_mode
