---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart windmill worker
  ansible.builtin.systemd:
    name: "{{ windmill_container_name }}"
    state: restarted
  when: not ansible_check_mode
